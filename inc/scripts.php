<?php
/**
 * Chargement des scripts et styles
 */

/*if ( ! defined( 'ABSPATH' ) ) {
     exit;
 }
 
 function theme_enqueue_assets() {
     wp_enqueue_style( 'theme-style', get_stylesheet_uri(), [], wp_get_theme()->get( 'Version' ) );
     wp_enqueue_script( 'theme-script', get_template_directory_uri() . '/js/main.js', ['jquery'], wp_get_theme()->get( 'Version' ), true );
 }
 add_action( 'wp_enqueue_scripts', 'theme_enqueue_assets', 10 );
 */
 
 ////////////////////////////////////////////////////